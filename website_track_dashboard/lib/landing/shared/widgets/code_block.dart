// Keep the legacy landing import pointed at the shared documentation editor.
// This prevents the landing page from drifting back to the old plain-text
// renderer while keeping one syntax-highlighting implementation.
export '../../../shared/widgets/code_block.dart';
